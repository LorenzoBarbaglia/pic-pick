/**
 * I colori condivisi che non passano dai token CSS dei mondi: il tratto delle
 * icone disegnate e le tinte semantiche fisse. Un posto solo, come LAYER.
 */

/** il tratto di tutta la UI disegnata (stone-200) */
export const LINE = '#e7e5e4'

/** il rosa del «Non passa» (rose-500), come terna r, g, b */
export const SKIP_TINT = '244, 63, 94'
